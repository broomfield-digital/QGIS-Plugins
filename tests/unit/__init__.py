"""Ring-1 unit tests: no QGIS, no GDAL, no numpy, no network.

Runnable on any Python 3.10+ with ``python -m unittest discover -s tests/unit -t .``.
Anything that needs a raster reader lives in ``tests/qgis``.
"""
